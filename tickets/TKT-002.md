---
id: TKT-002
title: "Add posts showcase page and nav link"
type: enhancement
status: proposed
priority: medium
app: default
branch: ""
created: 2026-04-13
updated: 2026-04-13
regression_risk: low
---

## Description

The posts/blog rendering pipeline shipped in TKT-001, but there's no way to discover it from the site. Other page types are documented in `page-types.mdx` (guide template, YAML examples, links to live demos). Posts needs the same treatment.

Two things are missing:

1. A showcase/documentation page explaining the posts feature, matching the style of `page-types.mdx` -- guide template, frontmatter reference, usage examples, link to the live `/posts/` listing as a demo.
2. A "Posts" link in the site nav (`site.config.ts`).

## What to build

- `src/content/pages/posts-guide.mdx` -- Documentation page for the posts feature. Use the `guide` template. Cover:
  - What posts are for (date-driven chronological content: blog, news, updates)
  - Required and optional frontmatter fields with YAML code block examples
  - How drafts work (`draft: true` filters from listing and individual routes)
  - Reading time (auto-calculated from body length)
  - Tags (displayed on cards and individual posts)
  - Link to `/posts/` as a live example
  - Use `Callout` components where appropriate (match existing doc style)

- Update `src/site.config.ts` -- Add `{ label: "Posts", href: "/posts" }` to the nav array in a sensible position (after existing page types or at the end).

- Add 1-2 more sample posts to `src/content/posts/` so the listing page looks less empty in the demo. Short, varied content (different tags, different lengths for varied reading times). These are framework demo content, not site-specific.

## Constraints

- Match the documentation style of `page-types.mdx` exactly -- guide template, structured headings, YAML code blocks, Callout components, links to live examples.
- No em dashes in copy.
- Framework files, not site-specific.
- Keep sample posts short and generic.

## Acceptance Criteria
- [ ] `src/content/pages/posts-guide.mdx` exists with guide template, documents all posts frontmatter fields
- [ ] `site.config.ts` nav includes a "Posts" link
- [ ] 1-2 additional sample posts exist in `src/content/posts/`
- [ ] `/posts/` listing shows multiple posts
- [ ] `npm run build` passes

---
<!-- Everything below is filled by the agent. Do not edit manually. -->

## Investigation
<!-- Agent fills this during /ticket-investigate -->

### Reference page: `page-types.mdx`
- **File:** `src/content/pages/page-types.mdx`
- Uses `template: "guide"`, `section: "reference"`, `nav_order: 30`
- Imports `Callout` from `../../components/Callout.astro` (line 11)
- Structure: intro paragraph, Callout noting the page itself uses the guide template, then sections per topic with `---` horizontal rules between them, YAML code blocks showing frontmatter, bold key/value lines for quick-reference details, links to live examples, and a quick-reference table at the end
- No em dashes used in the copy

### Navigation: `site.config.ts`
- **File:** `src/site.config.ts`, lines 25-33
- Nav array has 7 items: About, Building, Theming, Page Types, Controls, Deploy, Progress
- Each entry is `{ label: string, href: string }`
- "Posts" should be inserted after "Progress" (end of list) or after "Page Types" since it documents a content type

### Posts schema: `content.config.ts`
- **File:** `src/content.config.ts`, lines 53-66
- Posts collection fields:
  - `title` (string, required)
  - `description` (string, max 160 chars, required)
  - `date` (coerced Date, required)
  - `author` (string, default "Dan")
  - `tags` (string array, default [])
  - `hero_image` (string, optional)
  - `hero_alt` (string, optional)
  - `draft` (boolean, default false)
  - `og_image` (string, optional)

### Rendering layer
- **Listing:** `src/pages/posts/index.astro` -- filters drafts (line 7), sorts reverse-chronological (line 8), renders `PostCard` components
- **Individual:** `src/pages/posts/[slug].astro` -- uses `Content.astro` layout with `template="default"`, shows author and tags
- **PostCard:** `src/components/PostCard.astro` -- reading time calculated at line 13: `Math.max(1, Math.round(body.trim().split(/\s+/).length / 230))` (230 words/min)

### Existing sample post
- **File:** `src/content/posts/hello-world.mdx` -- single post, 2 short paragraphs, tags: ["welcome"], dated 2026-04-13
- The listing page will currently show only this one card

### Callout component
- **File:** `src/components/Callout.astro` -- supports `type` prop: "info" (default), "warning", "tip", "danger"; optional `title` prop
- Usage in page-types.mdx: `<Callout>` (no type = info default), `<Callout>` with inline bold for "New" callout

## Proposed Solution
<!-- Agent fills this during /ticket-investigate -->

Three deliverables, all additive (no existing files modified except `site.config.ts`):

1. **Posts guide page** (`src/content/pages/posts-guide.mdx`) -- A guide-template documentation page matching the style of `page-types.mdx`. Sections: overview of what posts are for, frontmatter reference with YAML code blocks, how drafts work, reading time explanation, tags, hero images, link to `/posts/` as live example. Uses Callout components.

2. **Nav link** -- Add `{ label: "Posts", href: "/posts" }` to the nav array in `site.config.ts`, positioned after "Page Types" since both are content-type documentation.

3. **Two additional sample posts** -- Short, varied demo content with different tags, dates, and lengths to show varied reading times on the listing page.

## Implementation Plan
<!-- Agent fills this during /ticket-investigate -->
- [ ] 1. **Create `src/content/pages/posts-guide.mdx`** -- New file. Frontmatter: `title: "Posts"`, `description: "Date-driven content for blogs, news, and updates -- how to create and configure posts in Loomwork."`, `section: "reference"`, `nav_order: 35`, `template: "guide"`, `date_created: 2026-04-13`, `date_updated: 2026-04-13`. Import Callout from `../../components/Callout.astro`. Content structure (matching page-types.mdx style):
  - Intro paragraph explaining posts are date-driven chronological content
  - `<Callout>` noting this page uses the guide template and linking to `/posts/` for the live listing
  - **## Getting Started** -- minimal frontmatter YAML code block showing required fields (title, description, date)
  - **## Frontmatter Reference** -- table of all fields from the posts schema (title, description, date, author, tags, hero_image, hero_alt, draft, og_image) with type, default, and description columns
  - **## Drafts** -- explain `draft: true` filters from listing and individual routes; YAML code block example; `<Callout type="tip">` noting drafts are excluded at build time
  - **## Reading Time** -- explain auto-calculation (body word count / 230, minimum 1 min), displayed on post cards
  - **## Tags** -- explain tags array, displayed on cards and individual post pages; YAML example
  - **## Hero Images** -- optional hero_image/hero_alt fields; YAML example
  - **## Live Example** -- link to [Posts listing](/posts) and note about the sample posts
- [ ] 2. **Update `src/site.config.ts`** -- Insert `{ label: "Posts", href: "/posts" },` into the `nav` array at line 30, after the "Page Types" entry. This shifts Controls, Deploy, Progress down by one position.
- [ ] 3. **Create `src/content/posts/building-with-collections.mdx`** -- Sample post. Frontmatter: `title: "Building with Content Collections"`, `description: "How Loomwork uses Astro Content Collections to turn MDX files into typed, validated pages."`, `date: 2026-04-10`, `author: "Dan"`, `tags: ["framework", "astro"]`, `draft: false`. Body: 3-4 short paragraphs (~150 words) about content collections, enough to produce a 1 min read.
- [ ] 4. **Create `src/content/posts/themes-and-customization.mdx`** -- Sample post. Frontmatter: `title: "Themes and Customization"`, `description: "A quick look at how Loomwork themes work and how to make a site your own."`, `date: 2026-04-07`, `author: "Dan"`, `tags: ["theming", "design"]`, `draft: false`. Body: 5-6 paragraphs (~300 words) about theming, enough to produce a 2 min read. Different tags from the other posts to show variety.
- [ ] 5. **Run `npm run build`** -- Verify the build passes with no errors. Confirm `/posts/` listing renders 3 posts, `/posts/building-with-collections` and `/posts/themes-and-customization` resolve, and `/posts-guide` renders the guide page.


## Dependencies

None.

## Files Changed
<!-- Agent fills this during implementation -->


## Test Report
<!-- Agent fills this after implementation -->


## Automated Checks
<!-- Agent fills this during /ticket-review or /ticket-chain -->


## Verification Checklist (for human)
<!-- Agent fills this during /ticket-review -->
- [ ] 


## Regression Report
<!-- Agent fills this before merge -->
